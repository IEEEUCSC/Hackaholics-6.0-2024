const Home = () => {
    return (
        <main className='flex flex-col h-screen items-center content-center justify-center text-slate-50 bg-secondary'>
      
        </main>

    );
};

export default Home