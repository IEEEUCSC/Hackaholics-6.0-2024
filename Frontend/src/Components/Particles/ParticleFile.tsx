import React from "react";
import ParticlesComponent from "./ParticleComponent";

import Particles from "react-tsparticles";
const ParticleFile= () => {
  return (
    <Particles params={ParticlesComponent}/>
    
  );
};

export default ParticleFile;
